#include <stdio.h>
int main()
{
    int a, b, sum;
    printf("Enter a Number: ");
    scanf("%d", &b);
    printf("Enter a Number: ");
    scanf("%d", &a);
    sum = a + b;
    printf("Sum of a and b is: %d", sum);
    return 0;
}